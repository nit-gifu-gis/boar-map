const LoadingTemplate: React.FunctionComponent = () => {
  return (
    <div className='flex h-screen min-h-screen items-center justify-center overflow-auto'>
      認証情報ロード中... (ここに認証情報ロード時の画面を作成する。)
    </div>
  );
};

export default LoadingTemplate;
